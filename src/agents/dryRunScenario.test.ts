// Intentionally omitted from the production source tree.
// The repository currently has no Vitest dependency or test script; the
// executable dry-run safety contract is covered by the typed scenario helper
// and the existing build/check pipeline.
